# Browser Extension Troubleshooting

## `window.ethereum` の競合エラー

スタックトレースに `chrome-extension://.../inpage.js` が含まれ、かつアプリケーションのソースコードには `window.ethereum` またはウォレットプロバイダーへの代入処理が存在しない場合、このエラーはサイトではなくブラウザ拡張機能のインジェクション処理から発生しています。

このダッシュボードはウォレット接続を実装していないため、拡張機能をオフにするか、その拡張機能を許可しないブラウザプロファイル・シークレットウィンドウで確認してください。複数のウォレット拡張機能を同時に有効にしている場合は、一時的に一つだけを残して再読み込みすると、`window.ethereum` の競合を切り分けられます。

